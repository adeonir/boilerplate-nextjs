import { Story, Meta } from '@storybook/react'

import { {{pascalCase name}}, Props } from '.'

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta<Props>

export const Basic: Story<Props> = (args) => <{{pascalCase name}} {...args} />
